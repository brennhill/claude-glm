#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
source "$SCRIPT_DIR/../scripts/lib/aiwrap-config.sh"

usage() {
  aiwrap_die "Usage: aiwrap <tool> <provider> [args...]"
}

[[ $# -ge 2 ]] || usage

tool=$1
provider=$2
shift 2

case "$tool" in
  claude)
    source "$SCRIPT_DIR/../scripts/lib/tools/claude.sh"
    ;;
  codex)
    source "$SCRIPT_DIR/../scripts/lib/tools/codex.sh"
    ;;
  *)
    aiwrap_die "Unsupported tool '$tool'. Supported tools: claude, codex"
    ;;
esac

case "$provider" in
  glm)
    source "$SCRIPT_DIR/../scripts/lib/providers/glm.sh"
    provider_glm_defaults
    ;;
  claude)
    source "$SCRIPT_DIR/../scripts/lib/providers/claude.sh"
    provider_claude_defaults
    ;;
  *)
    aiwrap_die "Unsupported provider '$provider'. Supported providers: glm, claude"
    ;;
esac

if ! "tool_${tool}_supports_provider" "$provider"; then
  aiwrap_die "Unsupported tool/provider pair: $tool -> $provider"
fi

binary=$("tool_${tool}_binary")
[[ -n "$binary" ]] || aiwrap_die "Required tool binary '$tool' was not found in PATH"

aiwrap_load_provider_config "$provider" || true
if ! aiwrap_has_usable_token "$provider_config_auth_token"; then
  provider_config_auth_token=$(aiwrap_prompt_for_token)
  aiwrap_write_provider_config "$provider" "$provider_config_auth_token"
  printf 'Saved token to %s\n' "$provider_config_file" >&2
fi

"tool_${tool}_exec" "$binary" "$@"
